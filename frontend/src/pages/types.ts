export type Picture = {
  src: string;
  alt: string;
  title: string;
  description: string;
}

export type Poem = {
  title: string;
  description: string;
}

export type BlogPost = {
  title: string;
  description: string;
  body: string;
}
